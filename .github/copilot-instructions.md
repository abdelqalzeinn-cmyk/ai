# Project instructions

- Keep the model and data pipeline understandable and dependency-light.
- Use ASCII source files unless a data format requires otherwise.
- Validate model changes with `python -m pytest -q`.
- Do not commit datasets, checkpoints, virtual environments, or secrets.
